<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.studyjams.s1.sj127.greenhouse">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ScrollingActivity_WeiHai"
            android:label="@string/title_activity_scrolling_activity__wei_hai"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".JingHua" />
        <activity android:name=".LvYi" />
        <activity android:name=".JiangXin" />
        <activity android:name=".MoreCool"></activity>
    </application>

</manifest>